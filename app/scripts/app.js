'use strict';

/**
 * @ngdoc overview
 * @name hahaApp
 * @description
 * # hahaApp
 *
 * Main module of the application.
 */
angular
  .module('hahaApp', [])
  		/*app.filter("sub",function(){
			return function(e){
				if(e.length<=10){
					return e
				}else{
					var arr = []
					arr.push(e.substring(0,10))
					var arr1 = (e.length)-(arr.join("").length)
					return arr+"..."
				}
			}
		}),
*/
